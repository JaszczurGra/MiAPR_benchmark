# RViz configs

`sim.launch.py` reuses the default RViz config shipped by `ur_moveit_config`
(MotionPlanning plugin). Drop a custom `*.rviz` here and point the launch file at it
if you want a benchmark-specific view (e.g. preset obstacle display).
